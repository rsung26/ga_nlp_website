<h2>My Entries</h2>
<table class="table">
	<% @user_text_entries.each do |t| %>
	<tr class="text_entry_<%=t.id%>">
		<td><strong><a href="<%= text_entry_path(t)%>"><%= t.title %></a></strong></td>
		<td><%= t.source %></td>
		<td><a href="<%= edit_text_entry_path(t)%>">EDIT</a></td>
		<td>
			<form action="<%=text_entry_path(t)%>" method="post">
				<input type="hidden" value="delete" name="_method">
				<input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
				<button class="btn-link">DELETE</button>
			</form>
		</td>
		<% if t.classifications.empty? %>
			<td>Not Yet Classified</td>
		<% else %>
			<td></td>
		<% end %>
	</tr>
	<% end %>
</table>
<form action="<%= new_text_entry_path %>" method="get">
	<input type="submit" class="btn btn-primary btn-large" value="Create New Entry">
</form>


<!-- <a href="<%= new_text_entry_path %>">Create a New Entry</a> -->